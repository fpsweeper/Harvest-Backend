<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Our application — INFO and above -->
    <logger name="com.fpsweeper.harvest" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE" />
    </logger>

    <!--
        BinanceApiService: silenced completely.
        451 geo-block errors fire every 5 min per bot in restricted regions.
        The CryptoCompare fallback in MarketDataService handles this transparently.
        Any real errors are re-caught and logged by MarketDataService with context.
    -->
    <logger name="com.fpsweeper.harvest.trading.binance.BinanceApiService" level="OFF" additivity="false" />

    <!-- Reduce framework noise -->
    <logger name="org.springframework"    level="WARN" />
    <logger name="org.hibernate"          level="WARN" />
    <logger name="com.zaxxer.hikari"      level="WARN" />

    <root level="WARN">
        <appender-ref ref="CONSOLE" />
    </root>

</configuration>